
export const getLoadingUI         = (state) => state.UI.loading;

export const getScreenFormats     = (state) => state.UI.screenFormats;

export const getAcceptedCookie    = (state) => state.UI.acceptedCookie;

export const getPolicy            = (state) => state.UI.policy;

export const getErrors            = (state) => state.UI.errors;

// Возвращает метку куда перейти с помощью Scroll 
export const getTargetScroll      = (state) => state.UI.targetScroll;

export const getSelectedGoodsType = (state) => state.UI.selectedGoodsType;